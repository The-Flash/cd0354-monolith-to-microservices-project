apiVersion: v1
kind: ConfigMap
metadata:
  name: udagram-env
data:
  port: "8080"
  url: "http://localhost:8080"
  aws_bucket: arn:aws:s3:::user2056-udagram-dev
  aws_region: us-east-1
  aws_profile: default
